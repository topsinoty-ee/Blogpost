{
  "name": "blogpost",
  "private": true,
  "workspaces": [
    "app/*",
    "lib/*"
  ],
  "version": "1.0.0",
  "description": "A monorepo for managing the blogpost application and library",
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "lerna": "^8.1.8"
  },
  "keywords": [
    "blog",
    "monorepo",
    "lerna"
  ],
  "author": "Promise Temitope",
  "license": "ISC"
}
